export const environment = {
  production: false,
  // Empty for now — mock services read from local data.
  // Point this at the future ASP.NET Core API base URL, e.g. 'https://api.aquashop.com/api'
  apiUrl: '',
  appName: 'AquaShop',
  currency: 'INR',
  currencySymbol: 'Rs.',
  taxRatePercent: 7,
};
